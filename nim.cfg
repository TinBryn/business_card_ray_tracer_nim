hints:on    # want to see that all of this actually happens
checks:off  # should be correct so don't add runtime checks
threads:on  # lots to do, spread the work
gc:arc      # not much allocation so use simple fast GC
opt:speed   # lots to do, make it fast
